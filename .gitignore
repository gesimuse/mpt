.venv/
__pycache__/
*.pyc
.env
!.env.example
.DS_Store
out/
.claude/
# tiktok.py's local worktree of the gh-pages branch -- a checkout of a different
# branch, never content that belongs in a main-branch commit.
.gh-pages-worktree/
# scripts/prepare_image_kernel.py's generated bundle -- rebuilt fresh every push.
kernel_build_imagegen/
# scripts/prepare_video_kernel.py's generated bundle -- same deal.
kernel_build_videogen/
